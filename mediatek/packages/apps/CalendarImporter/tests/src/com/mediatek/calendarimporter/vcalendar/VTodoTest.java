package com.mediatek.calendarimporter.vcalendar;

import java.util.List;

import android.util.Log;

import com.mediatek.calendarimporter.TestUtils;
import com.mediatek.vcalendar.ComponentParser;
import com.mediatek.vcalendar.component.Component;
import com.mediatek.vcalendar.component.VTodo;
import com.mediatek.vcalendar.property.Property;

import junit.framework.TestCase;

public class VTodoTest extends TestCase {

    @Override
    protected void setUp() throws Exception {
        super.setUp();
    }

    @Override
    protected void tearDown() throws Exception {
        super.tearDown();
    }

    public void test01_buildVTodoFromString(){
//        VTodo vTodo = (VTodo)ComponentParser.parse(TestUtils.vtodoString);
//
//        assertNotNull(vTodo.getDtStart());
//        assertNotNull(vTodo.getFirstProperty(Property.DTSTART));
//        assertNotNull(vTodo.getFirstProperty(Property.DESCRIPTION));
//        assertNotNull(vTodo.getFirstProperty(Property.SUMMARY));
//        assertNotNull(vTodo.getFirstProperty(Property.DUE));
//        assertNotNull(vTodo.getFirstProperty(Property.CATEGORIES));
//        assertNotNull(vTodo.getFirstProperty(Property.COMPLETED));
//        assertNotNull(vTodo.getFirstProperty(Property.PRIORITY));
    }

}
